import React from 'react';
import {render} from "react-dom";
import App from './Components/App';


// Connect and Rendering of all components
const rootElement = document.getElementById("root")
render( <App/>, rootElement);
